import { defineConfig } from 'windicss/helpers';

export default defineConfig({
    theme: {
        extend: {
            colors: {
                "sspsBlue": '#406280',
                "sspsBlueDark": '#243a55',
                "sspsGray": "#e6e6e6",
            }
        }
    }
});
